# Migration `20201209090048-changed-class-schema`

This migration has been generated by Devinda Senanayaka at 12/9/2020, 3:00:48 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Class" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "code" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "days" TEXT NOT NULL,
    "time" TEXT NOT NULL,
    "location" TEXT NOT NULL,
    "teacherId" INTEGER,

    FOREIGN KEY ("teacherId") REFERENCES "Teacher"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Class" ("id", "title", "teacherId") SELECT "id", "title", "teacherId" FROM "Class";
DROP TABLE "Class";
ALTER TABLE "new_Class" RENAME TO "Class";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201209080513-make-finance-optional..20201209090048-changed-class-schema
--- datamodel.dml
+++ datamodel.dml
@@ -3,16 +3,16 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model Student {
-  id                      Int     @id @default(autoincrement())
+  id                      Int     @id @default(autoincrement())  
+  studentID               Int
   profilePicURL           String
   creditsCompleted        Int
-  studentID               Int
   gender                  String
   dob                     String
   birthCountry            String
   birthState              String
@@ -89,16 +89,14 @@
 }
 model Class {
   id        Int       @id @default(autoincrement())
-  title     String?
-  detail    String?
-  startDate DateTime?
-  endDate   DateTime?
-  startTime Int?
-  endTime   Int?
-  teacherId Int?
-  teacher   Teacher?  @relation(fields: [teacherId], references: [id])
+  name      String
+  code      String
+  title     String
+  days      String
+  time      String
+  location  String
   students  Student[] @relation("RegistrationRecord")
 }
 model Teacher {
```


